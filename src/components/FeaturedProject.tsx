import { motion } from 'framer-motion';
import { ArrowUpRight, CheckCircle2, Cpu, Network, Shield, Webhook } from 'lucide-react';
const platformImg = `${import.meta.env.BASE_URL}images/project-platform.webp`;

const pipeline = [
  { label: 'Signals', tool: 'PPG & ECG Data', icon: Network },
  { label: 'Model', tool: 'TensorFlow / Keras', icon: Cpu },
  { label: 'Backend', tool: 'Flask REST API', icon: Webhook },
  { label: 'Security', tool: 'Environment Auth', icon: Shield },
];

const decisions = [
  ['Architecture', 'Built a modular Flask backend separating routing, model inference, and data validation.'],
  ['AI integration', 'Loaded pre-trained Keras models and scalers efficiently into the API lifecycle.'],
  ['Data handling', 'Implemented robust preprocessing to shape incoming signals for model prediction.'],
  ['Security', 'Removed hardcoded credentials and enforced environment variables.'],
];

export default function FeaturedProject() {
  return (
    <section id="featured" className="relative py-24 lg:py-28" aria-labelledby="featured-title">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex flex-col lg:flex-row lg:items-end lg:justify-between gap-6 mb-10"><div><span className="eyebrow">Featured case study · Backend/API</span><h2 id="featured-title" className="section-title mt-3">ABP Estimation API</h2><p className="theme-text-secondary max-w-2xl mt-4 text-lg">A backend architecture serving machine-learning predictions for Arterial Blood Pressure.</p></div><a href="https://github.com/Fatoomnoour/abp-api" target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 text-sm font-semibold text-primary-700 dark:text-primary-300 rounded-md">GitHub repository <ArrowUpRight className="w-4 h-4" aria-hidden="true" /></a></div>

        <div className="grid lg:grid-cols-[0.92fr_1.08fr] gap-8 items-stretch">
          <motion.div initial={{ opacity: 0, x: -18 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true, amount: 0.2 }} transition={{ duration: 0.45 }} className="technical-panel p-6 md:p-8">
            <div className="flex items-center justify-between mb-8"><div><p className="text-xs uppercase tracking-[0.2em] text-primary-700 dark:text-primary-300">System architecture</p><p className="text-sm theme-text-muted font-mono mt-2">abp_inference_api</p></div><span className="status-dot"><span aria-hidden="true" /> evidence-led</span></div>
            <div className="relative h-48 mb-8 rounded-xl overflow-hidden border theme-border bg-slate-100 dark:bg-slate-900"><img src={platformImg} alt="Abstract preview of the ABP estimation API architecture" loading="lazy" decoding="async" className="w-full h-full object-cover opacity-60 mix-blend-multiply dark:mix-blend-normal" /><div className="absolute inset-0 bg-gradient-to-t from-white dark:from-[#1E293B] to-transparent" aria-hidden="true" /></div>
            <div className="space-y-3" aria-label="ABP API architecture stages">{pipeline.map((stage, index) => { const Icon = stage.icon; return <div key={stage.label} className="pipeline-row"><div className="pipeline-index">0{index + 1}</div><div className="pipeline-icon"><Icon className="w-4 h-4 text-primary-700 dark:text-primary-300" aria-hidden="true" /></div><div className="min-w-0"><p className="text-xs uppercase tracking-widest theme-text-muted">{stage.label}</p><p className="font-semibold mt-1 truncate">{stage.tool}</p></div>{index < pipeline.length - 1 && <div className="pipeline-connector" aria-hidden="true" />}</div>; })}</div>
          </motion.div>

          <motion.div initial={{ opacity: 0, x: 18 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true, amount: 0.2 }} transition={{ duration: 0.45, delay: 0.08 }} className="space-y-5">
            <div className="theme-bg-card border theme-border rounded-2xl p-6 md:p-8 space-y-4"><p className="eyebrow">Case study</p><p><strong>Problem:</strong> <span className="theme-text-secondary">Healthcare prediction models need a secure and dependable backend for inference requests.</span></p><p><strong>Solution:</strong> <span className="theme-text-secondary">Wrapped a deep-learning model in a production-minded REST API that validates signal data and returns estimated blood-pressure values.</span></p><p><strong>My contribution:</strong> <span className="theme-text-secondary">Designed the Flask structure, model lifecycle, preprocessing flow, and environment-based security.</span></p><p><strong>Outcome:</strong> <span className="theme-text-secondary">A clear API architecture demonstrating how ML models can be served through a maintainable backend.</span></p><div className="flex flex-wrap gap-2 pt-2">{['Python', 'Flask', 'TensorFlow', 'Keras', 'REST API', 'Data Validation'].map((tool) => <span key={tool} className="tech-chip">{tool}</span>)}</div><a href="https://github.com/Fatoomnoour/abp-api" target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 text-sm font-semibold text-primary-700 dark:text-primary-300 pt-2 rounded-md">GitHub <ArrowUpRight className="w-4 h-4" aria-hidden="true" /></a></div>
            <div className="theme-bg-card border theme-border rounded-2xl p-6 md:p-8"><p className="eyebrow">Engineering decisions</p><div className="mt-5 grid sm:grid-cols-2 gap-x-6 gap-y-5">{decisions.map(([tool, explanation]) => <div key={tool} className="flex items-start gap-3"><CheckCircle2 className="w-4 h-4 text-primary-700 dark:text-primary-300 mt-1 shrink-0" aria-hidden="true" /><p className="text-sm leading-relaxed theme-text-secondary"><strong className="theme-text">{tool}:</strong> {explanation}</p></div>)}</div></div>
          </motion.div>
        </div>

        <div className="flex flex-col lg:flex-row lg:items-end lg:justify-between gap-6 mt-28 mb-10"><div><span className="eyebrow">Featured case study · AI & ML</span><h2 className="section-title mt-3">Stress Detection ML Pipeline</h2><p className="theme-text-secondary max-w-2xl mt-4 text-lg">A structured machine-learning workflow from raw data collection to model evaluation.</p></div><a href="https://github.com/Fatoomnoour/Stress-Detection" target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 text-sm font-semibold text-primary-700 dark:text-primary-300 rounded-md">GitHub repository <ArrowUpRight className="w-4 h-4" aria-hidden="true" /></a></div>
        <div className="grid lg:grid-cols-[1.08fr_0.92fr] gap-8 items-stretch"><div className="theme-bg-card border theme-border rounded-2xl p-6 md:p-8"><p className="eyebrow">Case study</p><div className="mt-5 flex flex-wrap items-center gap-3 text-sm font-medium"><span className="px-3 py-1.5 rounded-lg theme-bg-card border theme-border">Data collection</span><span className="theme-text-muted" aria-hidden="true">→</span><span className="px-3 py-1.5 rounded-lg theme-bg-card border theme-border">Cleaning</span><span className="theme-text-muted" aria-hidden="true">→</span><span className="px-3 py-1.5 rounded-lg theme-bg-card border theme-border">Feature engineering</span><span className="theme-text-muted" aria-hidden="true">→</span><span className="px-3 py-1.5 rounded-lg theme-bg-card border theme-border">Training</span><span className="theme-text-muted" aria-hidden="true">→</span><span className="px-3 py-1.5 rounded-lg theme-bg-card border theme-border">Evaluation</span></div><div className="space-y-4 text-sm leading-relaxed mt-6"><p><strong>Problem:</strong> <span className="theme-text-secondary">Noisy physiological data needs a reproducible workflow before it can support useful predictions.</span></p><p><strong>Solution:</strong> <span className="theme-text-secondary">Built an EDA and modeling workflow with Pandas and Scikit-learn.</span></p><p><strong>My contribution:</strong> <span className="theme-text-secondary">Handled cleaning, feature engineering, model comparison, and evaluation.</span></p><p><strong>Outcome:</strong> <span className="theme-text-secondary">A documented predictive pipeline with established evaluation metrics.</span></p></div><div className="flex flex-wrap gap-2 mt-6">{['Python', 'Pandas', 'Scikit-learn', 'EDA', 'Feature Engineering'].map((tool) => <span key={tool} className="tech-chip">{tool}</span>)}</div></div><div className="technical-panel p-6 md:p-8"><p className="text-xs uppercase tracking-[0.2em] text-primary-700 dark:text-primary-300">ML pipeline</p><p className="text-sm theme-text-muted font-mono mt-2">stress_detection_model</p><div className="space-y-4 mt-8"><div className="p-4 theme-bg-card border theme-border rounded-xl"><p className="text-xs uppercase tracking-widest theme-text-muted mb-1">Dataset</p><p className="font-medium text-sm">Physiological sensor data processed via Pandas</p></div><div className="p-4 theme-bg-card border theme-border rounded-xl"><p className="text-xs uppercase tracking-widest theme-text-muted mb-1">Models applied</p><p className="font-medium text-sm">Classification algorithms evaluated for accuracy</p></div><div className="p-4 theme-bg-card border theme-border rounded-xl"><p className="text-xs uppercase tracking-widest theme-text-muted mb-1">Outcome</p><p className="font-medium text-sm">Predictive pipeline with established evaluation metrics</p></div></div><a href="https://github.com/Fatoomnoour/Stress-Detection" target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 text-sm font-semibold text-primary-700 dark:text-primary-300 mt-8 rounded-md">GitHub <ArrowUpRight className="w-4 h-4" aria-hidden="true" /></a></div></div>
      </div>
    </section>
  );
}
